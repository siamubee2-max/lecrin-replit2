/** @type {const} */
const themeColors = {
  // Luxurious gold and dark theme for jewelry app
  primary: { light: '#D4AF37', dark: '#FFD700' },      // Gold accent
  background: { light: '#FFFFFF', dark: '#0A0A0A' },   // Clean white / Deep black
  surface: { light: '#F8F6F0', dark: '#1A1A1A' },      // Cream / Dark gray
  foreground: { light: '#1A1A1A', dark: '#F5F5F5' },   // Dark text / Light text
  muted: { light: '#6B6B6B', dark: '#A0A0A0' },        // Secondary text
  border: { light: '#E5E0D5', dark: '#2A2A2A' },       // Subtle borders
  success: { light: '#22C55E', dark: '#4ADE80' },      // Success states
  warning: { light: '#F59E0B', dark: '#FBBF24' },      // Warning states
  error: { light: '#EF4444', dark: '#F87171' },        // Error states
};

module.exports = { themeColors };
